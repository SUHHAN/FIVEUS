using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class fififififi : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // 이러면 수정이 완료된건가?
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
